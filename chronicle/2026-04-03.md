# Session — 2026-04-03 ~morning
**Project:** Associative Memory
**Context:** Capturing voice notes on compute architecture direction

---

## ~morning — [Discovery]

**What:** Captured Wisdom's voice notes on a new research direction: custom compute architectures optimized for the AM graph.
**Why:** The core AM architecture will eventually need hardware/software that reflects its structure, not just runs on commodity GPUs. This is the long-term vision beyond local-cognition.
**Means:** AM now has three ambition levels: (1) local LLM + graph on commodity hardware (local-cognition), (2) separated memory-engine and logic-engine software architecture, (3) custom compute hardware. The two-system design — memory engine for read/write/explore, logic engine for reasoning, pure math engine for calculation — is a significant architectural vision that shapes how we think about what the LLM's role actually is.
**Files:** `research/compute-architecture/notes.md` (created), `CLAUDE.md` (updated)

Key insight: the LLM's weights should serve to *read* the memories, not *store* knowledge. This is the logical extension of "graph as mind" — even the neural network itself becomes a tool for the graph, not the other way around.

TODO noted: search Inventions Portfolio and ChatGPT Archive for Wisdom's prior computing architecture invention notes.

---
